import "./globals.css";


export const metadata = {
  title: "Amplify Gen 2 Multi Auth Rules",
  description: "How do I get this to work?",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}
